import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#FD5D5D",
    },
    secondary: {
      main: "#C0EDA6",
    },
  },
});

export default theme;
